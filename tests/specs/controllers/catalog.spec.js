var CatalogController = require('../../../api/controllers/CatalogController');
var sinon = require('sinon');
var assert = require('chai').assert;

describe('The Catalog Controller', function () {
    describe('when we load the catalog page', function () {
        it('should render the view', function (done) {
            //var view = sinon.spy();
            //CatalogController.init(null, {
            //    view: view
            //});
            done();
            //assert.ok(view.called);
        });
    });
});
